{
  "buildCommand": "npx vite build && rm -rf public && cp -r dist public",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
